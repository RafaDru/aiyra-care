# Investigação — a9cb7f94-a13f-43af-8ab4-d4aec53b6213

- **investigationId:** `051d1a71-ffc9-4e68-9802-09054bb9a2c3`
- **Categoria:** `technical_bug`
- **Rota:** `/`
- **Fingerprint:** *(ausente no webhook — `topFingerprint: null`)*
- **Tier:** 0 (rascunho automático)
- **Gatilho:** `auto`
- **Ambiente:** `integration` (`environment.deploymentTier`)
- **API:** `http://127.0.0.1:3010` (`environment.apiPublicUrl`)
- **Consentimento técnico:** sim (`consentTechnical: true`)
- **Acesso ao perfil:** não (`consentProfileAccess: false`)
- **Notas ops:** *(não enviadas no payload)*

## Hipóteses

1. **Falha ao carregar lista de pacientes / círculos no Dashboard (principal)** — A rota `/` renderiza `Dashboard` (`packages/web/src/pages/dashboard.tsx`), que no mount chama `api.patients.list()` e `api.careCircles.dashboard()`. Rejeição de `patients.list` preenche `loadError` com `Alert` de erro; falha só em círculos é silenciosa (`circleGroups` vazio). Se a API estiver indisponível ou retornar 5xx/401 transitório, o usuário vê erro na home sem necessariamente haver `client_errors` recentes no bundle (janela 24h / dedupe 15s em `reportClientError`).
2. **Chamado sem correlação automática de erro** — `topFingerprint` é derivado do primeiro item de `diagnostic_context.recentClientErrors` (`extractTopFingerprint` em `support-report-dispatch.ts`). `null` indica array vazio ou sem fingerprint no momento do submit: o relato pode ser intermitente, anterior à janela, ou sintoma visual (loading prolongado, estado vazio) sem exceção catalogada.
3. **Incidente paralelo na mesma rota (integration, set/2026)** — Outros reportes em `/` no mesmo ambiente correlacionaram fingerprint `036967be…` → `ui|ui_boundary|Error` (React `AppErrorBoundary`). Este ticket **não** traz esse fingerprint; pode ser caso distinto ou mesma classe de problema sem novo evento na hora do reporte.

## Evidências no repo

| Verificação | Resultado |
|-------------|-----------|
| Mapeamento rota | `App.tsx`: `path="/"` → `Dashboard` em `packages/web/src/pages/dashboard.tsx` |
| Carga inicial | `load()` — `Promise.allSettled([api.patients.list(), api.careCircles.dashboard()])` |
| Bundle diagnóstico | `docs/features/support-user-reports.md` — até 10 `client_errors` (24h); ausência explica `topFingerprint: null` |
| Erro UI global | `AppErrorBoundary` → `reportUiBoundaryError` → fingerprint `ui\|ui_boundary\|…` |
| Playbook tier | `investigation.tier: 0` — sem PR; triagem humana |

## Próximo passo humano

1. No console ops, abrir o detalhe do chamado e ler a **descrição livre** do usuário (não enviada ao agente por minimização de PHI).
2. Com acesso ao PG de integração:

```sql
SELECT id, category, route, status, analysis_status,
       diagnostic_context->'recentClientErrors' AS recent_errors,
       diagnostic_context->'recentProductEvents' AS recent_events,
       created_at
FROM support_reports
WHERE id = 'a9cb7f94-a13f-43af-8ab4-d4aec53b6213';
```

3. Se `recent_errors` estiver vazio, checar `GET /health` e logs da API no horário `2026-09-16T02:33:57Z` ±15 min; reproduzir abertura de `/` após login.
4. Se aparecer `ui_boundary`, inspecionar stack no `client_errors` correlato e componente em `properties.component`.
5. Marcar análise **Concluída** no console após validação; escalar para Tier 1 só se correção óbvia nos gates (`TIER1_GATES.md`).

## Console

http://127.0.0.1:3013?tab=incidentes&investigationId=051d1a71-ffc9-4e68-9802-09054bb9a2c3&reportId=a9cb7f94-a13f-43af-8ab4-d4aec53b6213

## Remediação (resumo agente)

Rascunho Tier 0: home (`Dashboard`) com bug técnico reportado sem fingerprint; priorizar leitura humana da descrição + SQL acima e saúde da API/listagem de pacientes. Nenhuma alteração de código nesta execução.
