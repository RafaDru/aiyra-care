# Investigação — a9cb7f94-a13f-43af-8ab4-d4aec53b6213

- **investigationId:** 051d1a71-ffc9-4e68-9802-09054bb9a2c3
- **Categoria:** technical_bug
- **Rota:** `/`
- **Fingerprint:** *(null — bundle sem `recentClientErrors` nas últimas 24h)*
- **Tier:** 0 (rascunho automático)
- **Gatilho:** auto
- **Ambiente:** integration (`environment.deploymentTier`)
- **API:** http://127.0.0.1:3010 (`environment.apiPublicUrl`)
- **Consentimento técnico:** sim · **Acesso ao perfil:** não
- **Notas ops:** *(não informadas no payload)*

## Contexto

Reporte manual na **home/dashboard** (`/` → `packages/web/src/pages/dashboard.tsx`). O webhook não trouxe `topFingerprint` apesar de `consentTechnical: true`, o que indica que o bundle `diagnostic_context.recentClientErrors` estava vazio no momento do submit — ou seja, **nenhum erro HTTP/rede/JS catalogado** nas últimas 24h correlacionado à conta.

A rota `/` mapeia para feature `dashboard` (`screen-feature-key.ts`, `client-error-fingerprint.ts`).

## Hipóteses

1. **Falha funcional silenciosa no bloco «Hoje» (sem exceção catalogada)** — `DashboardDayToDaySection` só renderiza `WalletTodayPanel` quando `useAvaPatientLens` já resolveu `patientId`. Enquanto carrega, retorna `null` (bloco ausente). Em `WalletTodayPanel.load()`, erros de API são engolidos (`catch { setItems([]) }`) e o usuário vê estado vazio em vez de alerta — comportamento percebido como bug técnico sem gerar `client_errors` se as chamadas retornarem 200 com listas vazias ou falharem de forma já tratada upstream.

2. **Layout por círculos familiares degradado sem erro explícito** — `dashboard.tsx` chama `Promise.allSettled([api.patients.list(), api.careCircles.dashboard()])`. Falha em `careCircles.dashboard()` zera `circleGroups` silenciosamente (`circlesResult.status !== 'fulfilled' → []`), revertendo para agrupamento por idade. Usuário com círculos configurados pode ver perfis «fora de lugar» sem fingerprint.

3. **Relato sem correlação técnica automática (triagem humana necessária)** — Ausência de fingerprint + rota genérica `/` + sem `operatorNotes` limita inferência. Pode ser edge case de sessão/auth, copy/i18n, ou classificação `technical_bug` para confusão de UX. Requer leitura da descrição do usuário (campo sensível — só operador autorizado) e inspeção do bundle PG.

## Evidências no repo

- Rota `/` → `Dashboard` em `packages/web/src/App.tsx`.
- Bloco «Hoje» no dashboard: `packages/web/src/components/dashboard/DashboardDayToDaySection.tsx` → `WalletTodayPanel`.
- `WalletTodayPanel` engole falhas de carga:

```130:134:packages/web/src/components/patient/WalletTodayPanel.tsx
    } catch {
      setItems([])
    } finally {
      setLoading(false)
    }
```

- `DashboardDayToDaySection` oculta o painel durante loading ou sem `patientId`:

```6:8:packages/web/src/components/dashboard/DashboardDayToDaySection.tsx
  const { patients, patientId, routePatientId, loading, setPatientId } = useAvaPatientLens()

  if (loading || !patientId || patients.length === 0) return null
```

- Dupla carga de pacientes na mesma tela: `dashboard.tsx` (`load()` → `api.patients.list()`) **e** `useAvaPatientLens` (segundo `api.patients.list()` independente) — possível race/flicker no bloco «Hoje».
- `topFingerprint` extraído só se `diagnostic_context.recentClientErrors[0].fingerprint` existe (`support-report-dispatch.ts` → `extractTopFingerprint`).
- E2E espera bloco «Hoje» visível no dashboard: `packages/web/e2e/suites/family-day-timeline.spec.ts` («bloco Hoje visível no dashboard»).
- Triagem `technical_bug` sem fingerprint: runbook recomenda olhar rota + erros; aqui a rota aponta dashboard/home.

## Próximo passo humano

1. **Inspecionar bundle (ambiente integration autorizado)** — sem expor PHI no Slack:

```sql
SELECT id, category, route, consent_technical, analysis_status,
       diagnostic_context->'recentProductEvents' AS events,
       diagnostic_context->'recentClientErrors' AS errors,
       diagnostic_context->'client' AS client,
       created_at
FROM support_reports
WHERE id = 'a9cb7f94-a13f-43af-8ab4-d4aec53b6213';
```

2. **Ler descrição livre** (pode conter dado sensível — só operador): mesma linha, coluna `description`.

3. **Reproduzir na home** com conta do reporte (requer `consent_profile_access` ou conta conhecida):
   - Login → `/` → verificar se `.wallet-today-panel` aparece (E2E: «Hoje» + «Registro rápido»).
   - DevTools Network: `GET /patients`, `GET /care-circles/dashboard`, `GET /scheduled-events`, `/care-reminders/pending`, `/measurements/timeline`.
   - Se vazio indevido: confirmar se APIs retornam 200 com dados vs. erro silenciado no `catch`.

4. **Se confirmado swallow de erro no «Hoje»** — candidato Tier 1: propagar `loadError` em `WalletTodayPanel` (allowlist web) + suite `family-day-timeline`.

5. **Marcar análise no console** após revisão: aba Issues → investigation `051d1a71-ffc9-4e68-9802-09054bb9a2c3`.

## Console

http://127.0.0.1:3013?tab=issues&investigationId=051d1a71-ffc9-4e68-9802-09054bb9a2c3&reportId=a9cb7f94-a13f-43af-8ab4-d4aec53b6213
