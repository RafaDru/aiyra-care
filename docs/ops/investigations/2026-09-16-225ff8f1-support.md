# Investigação — 90a796de-d46e-4aae-af1a-55de40bff325

- **investigationId:** `225ff8f1-4420-484e-9cc3-d8b1b29981bd`
- **Categoria:** `technical_bug`
- **Rota:** `/` (Dashboard autenticado — `packages/web/src/pages/dashboard.tsx`)
- **Fingerprint:** `036967be53b57d23` → decodificado: `ui|ui_boundary|Error`
- **Tier:** 0 (rascunho automático — sem PR de produto)
- **Gatilho:** `auto`
- **Ambiente:** `integration` (`environment.deploymentTier`)
- **API:** `http://127.0.0.1:3010` (`environment.apiPublicUrl`)
- **Consentimento técnico:** sim (`consentTechnical: true`)
- **Notas ops:** (nenhuma no payload)

## Correlação

Mesmo fingerprint e rota em outros chamados recentes (cluster `ui|ui_boundary|Error` na home): investigações `7cae1d6f`, `96a2e898`, `1b55aea2`, `07c77172` — sugere falha recorrente no subtree do Dashboard ou boundary global, não ruído de um único usuário.

## Hipóteses

1. **React Error Boundary na árvore do Dashboard (mais provável)** — `AppErrorBoundary` (`packages/web/src/components/errors/AppErrorBoundary.tsx`) chama `reportUiBoundaryError` com `feature: 'ui'` fixo e `errorCode` = `error.name` (aqui `Error`). Rota `/` mapeia telemetria de feature `dashboard`, mas o fingerprint do boundary **não** usa `deriveFeatureFromRoute`, por isso o hash bate em `ui` e não em `dashboard`.
2. **Subcomponente com pacientes carregados** — Com `patients.length > 0`, o Dashboard monta `DashboardDayToDaySection` → `useAvaPatientLens` + `WalletTodayPanel` (`packages/web/src/components/dashboard/DashboardDayToDaySection.tsx`). Exceção de render/hook nesse bloco ainda é capturada pelo boundary raiz e gera o mesmo fingerprint genérico.
3. **Dados de círculos / lista inconsistentes** — `api.careCircles.dashboard()` alimenta `groupedByCircle`; falha de shape (ex.: `patientIds` órfãos) costuma degradar para lista vazia, mas edge case em `renderAgeSections` / `PatientCard` (ex.: `ageCategory` fora do mapa) pode lançar se algum campo obrigatório vier `null` da API.

## Evidências no repo

- Fingerprint: `computeClientErrorFingerprint('ui','ui_boundary','Error')` → `036967be53b57d23` (`packages/web/src/lib/client-error-fingerprint.ts`).
- Boundary + telemetria: `reportUiBoundaryError` em `packages/web/src/lib/client-errors.ts` (feature hardcoded `ui`).
- Rota `/` → `<Dashboard />` em `packages/web/src/App.tsx`.
- Triagem `technical_bug`: `docs/ops/SUPPORT_REPORTS.md` — cruzar `client_errors` + rota.
- `properties.component` no POST de `client_errors` vem do primeiro frame de `componentStack` no `componentDidCatch` — **chave para localizar o componente** sem PHI.

## Próximo passo humano

1. No PG **integration** (não usar dados clínicos na saída do agente):

```sql
-- Erros do chamado (janela ±15 min)
SELECT ce.fingerprint, ce.error_code, ce.error_kind, ce.feature, ce.route,
       ce.properties->>'component' AS react_component,
       ce.created_at
FROM support_reports sr
JOIN client_errors ce
  ON ce.account_id = sr.account_id
 AND ce.created_at BETWEEN sr.created_at - INTERVAL '15 minutes'
                       AND sr.created_at + INTERVAL '5 minutes'
WHERE sr.id = '90a796de-d46e-4aae-af1a-55de40bff325'
ORDER BY ce.created_at DESC;

-- Volume cluster fingerprint 24h
SELECT COUNT(*) AS n, COUNT(DISTINCT account_id) AS accounts
FROM client_errors
WHERE fingerprint = '036967be53b57d23'
  AND created_at > NOW() - INTERVAL '24 hours';
```

2. Repro local: login → `/` com conta que tinha pacientes; DevTools → confirmar overlay do boundary; opcional: React DevTools para árvore no momento do throw.
3. Se `react_component` apontar para `WalletTodayPanel` / lente Ava, priorizar repro com ≥1 paciente e revisar últimas mudanças nesse módulo.
4. Melhoria futura (fora Tier 0): passar `deriveFeatureFromRoute(window.location.pathname)` em `reportUiBoundaryError` para separar `dashboard` vs outras rotas no ops.

## Console

http://127.0.0.1:3013?tab=incidentes&investigationId=225ff8f1-4420-484e-9cc3-d8b1b29981bd&reportId=90a796de-d46e-4aae-af1a-55de40bff325
