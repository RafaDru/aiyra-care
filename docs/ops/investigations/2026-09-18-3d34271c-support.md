# Investigação — d2cd8755-7542-4d2c-b0a3-10e691bcc6ec

- **investigationId:** 3d34271c-162b-4b3f-b610-f068059cb026
- **Categoria:** technical_bug
- **Rota:** `/`
- **Fingerprint:** _(ausente no webhook — `topFingerprint: null`)_
- **Tier:** 0 (rascunho automático)
- **Gatilho:** auto
- **Ambiente:** preview (`environment.deploymentTier`)
- **API:** http://127.0.0.1:3020 (`environment.apiPublicUrl`)
- **Consentimento técnico:** sim (`consentTechnical: true`)
- **Consentimento perfil:** não (`consentProfileAccess: false`)
- **Notas ops:** _(não informadas no payload)_

## Contexto

Reporte manual na **home** (`/` → `packages/web/src/pages/dashboard.tsx`). O webhook não trouxe fingerprint de `client_errors`, apesar do bundle técnico estar habilitado — indica ausência de erros catalogados recentes na conta **ou** falha que não passa por `reportClientError` / `AppErrorBoundary`.

Mapeamento de rota:

| Rota | Componente principal |
|------|----------------------|
| `/` | `Dashboard` + `DashboardDayToDaySection` (`WalletTodayPanel`) |

## Hipóteses

1. **Falha silenciosa no bloco «Hoje» (`WalletTodayPanel`)** — em `load()`, exceções de `scheduledEvents`, `careReminders` ou `measurements.timeline` caem em `catch` que apenas zera a lista, sem Alert nem telemetria `client_errors`. O usuário vê home aparentemente ok com seção «Hoje» vazia ou incompleta e classifica como bug técnico.
2. **Falha parcial no carregamento do dashboard** — `Promise.allSettled` trata rejeição de `api.careCircles.dashboard()` como círculos vazios (`[]`) sem alerta, enquanto a grade de pacientes renderiza; pode parecer «dado quebrado» ou layout errado na `/` sem fingerprint se a API de círculos não foi chamada com status reportado na janela dos últimos 10 erros.
3. **Bug lógico / intermitente sem exceção** — categoria `technical_bug` com bundle vazio de fingerprint: reprodução exige `diagnostic_context` no PG (eventos allowlist, erros, `client`) e descrição livre do usuário; sem `consent_profile_access`, triagem humana no preview com operador autenticado.

## Evidências no repo

- Rota `/` registrada em `packages/web/src/App.tsx` → `Dashboard`.
- Carga inicial: `api.patients.list()` + `api.careCircles.dashboard()` — falha de pacientes gera `loadError` Alert; falha só de círculos é silenciosa (`dashboard.tsx`, linhas 43–59).
- `DashboardDayToDaySection` só monta `WalletTodayPanel` se há pacientes; erros internos do painel não propagam para o dashboard (`DashboardDayToDaySection.tsx`; `WalletTodayPanel.tsx` `load()` ~131–135).
- Reporte captura `location.pathname` no modal (`SupportReportModal.tsx`) — rota `/` é confiável para «estava na home».
- Telemetria: erros de API/rede em `request()` chamam `reportApiClientError` / `reportNetworkClientError` (`api.ts`); React usa `AppErrorBoundary` → `reportUiBoundaryError` (`ui|ui_boundary|…`). Ausência de `topFingerprint` sugere que nenhum desses caminhos disparou na janela consultada pelo backend (`fetchRecentClientErrors`, limite 10 — `support-report.service.ts`).

## Próximo passo humano

1. No console ops (link abaixo), abrir o chamado `d2cd8755-7542-4d2c-b0a3-10e691bcc6ec` e ler descrição do usuário (não enviada ao agente por LGPD).
2. No PG **preview** (`aiyracare_preview`), inspecionar bundle (sem exportar PHI):

```sql
SELECT id, category, route, description,
       diagnostic_context->'recentClientErrors' AS errors,
       diagnostic_context->'recentProductEvents' AS events,
       diagnostic_context->'client' AS client,
       created_at
FROM support_reports
WHERE id = 'd2cd8755-7542-4d2c-b0a3-10e691bcc6ec';
```

3. Se `recentClientErrors` vazio: reproduzir na home preview com DevTools (Network) em `/patients`, `/care-circles/...`, endpoints do `WalletTodayPanel`; confirmar hipótese 1 ou 2.
4. Se aparecer fingerprint `036967be53b57d23` (`ui|ui_boundary|Error`): correlacionar com `AppErrorBoundary` e stack no mesmo intervalo (`client_errors` + `report_id`).
5. Marcar análise concluída no console ou re-disparar agente com **notas ops** (passos de repro, screenshot ops) se Tier 1 for adequado depois.

## Console

http://127.0.0.1:3023?tab=issues&investigationId=3d34271c-162b-4b3f-b610-f068059cb026&reportId=d2cd8755-7542-4d2c-b0a3-10e691bcc6ec
