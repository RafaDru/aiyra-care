# Investigação — 7f7f6760-52e7-43f4-9070-b9ed28c4e1ba

- **investigationId:** `1b55aea2-b996-44a4-bdd3-6677aaf44d26`
- **Categoria:** `technical_bug`
- **Rota:** `/`
- **Fingerprint:** `036967be53b57d23` → `ui|ui_boundary|Error`
- **Tier:** 0 (rascunho automático)
- **Gatilho:** `auto`
- **Ambiente:** `integration` (`environment.deploymentTier`)
- **API:** `http://127.0.0.1:3010` (`environment.apiPublicUrl`)
- **Notas ops:** *(ausente no payload)*

## Hipóteses

1. **Erro de renderização não tratado no Dashboard (principal)** — A rota `/` monta `Dashboard` (`packages/web/src/pages/dashboard.tsx`) dentro de `AppErrorBoundary` (`main.tsx`). O fingerprint fixa `feature=ui`, `errorKind=ui_boundary`, `errorCode=Error` (nome da exceção React), típico de `componentDidCatch` em `AppErrorBoundary.tsx` (linha ~28). Falhas em `load()` já viram `loadError` na UI; um throw em render/`useMemo` (ex.: agrupamento por círculo, cards de paciente) cai no boundary genérico.
2. **Cluster recorrente no mesmo ambiente** — O mesmo fingerprint na rota `/` em integration aparece em outros chamados da fila (ex.: investigation `7cae1d6f`, `96a2e898`, `07c77172`, `225ff8f1`). Sugere causa compartilhada (build local, dados de demo, ou componente filho do dashboard) e não relato isolado de copy/UX.
3. **Sessão/auth ou provider acima do Dashboard** — Menos provável para este fingerprint (seria `api`/`network` se só falhasse fetch), mas `ActiveCareCircleProvider` / `AuthProvider` envolvem a árvore; exceção durante hidratação pós-login em `/` ainda seria reportada como `ui_boundary` se estourar abaixo do boundary global.

## Evidências no repo

| Verificação | Resultado |
|-------------|-----------|
| Fingerprint | `SHA-256("ui\|ui_boundary\|error")[0:16]` = `036967be53b57d23` (`client-error-fingerprint.ts`) |
| Emissão do erro | `reportUiBoundaryError` → `feature: 'ui'` (`client-errors.ts`) |
| Rota `/` | `App.tsx` → `<Route path="/" element={<Dashboard />} />` (auth + compliance) |
| Boundary | `AppErrorBoundary` reporta `error.name \|\| 'Error'` + `properties.component` (1ª linha útil do `componentStack`) |
| Triagem docs | `technical_bug` → correlacionar `client_errors` + rota (`docs/ops/SUPPORT_REPORTS.md`) |
| Tier 0 | Sem alteração de produto nesta execução; ver PR draft de referência cluster `7cae1d6f` (#76) |

## Próximo passo humano

1. No console integration (`dashboardUrl` abaixo), abrir o chamado `7f7f6760-52e7-43f4-9070-b9ed28c4e1ba` e o bundle técnico (`consentTechnical=true`).
2. Correlacionar erros na janela do submit (sem expor PHI na automação):

```sql
SELECT ce.fingerprint,
       ce.error_kind,
       ce.error_code,
       ce.route,
       ce.properties->>'component' AS react_component_hint,
       ce.created_at
FROM support_reports sr
JOIN client_errors ce
  ON ce.created_at BETWEEN sr.created_at - INTERVAL '2 hours' AND sr.created_at + INTERVAL '15 minutes'
WHERE sr.id = '7f7f6760-52e7-43f4-9070-b9ed28c4e1ba'
  AND ce.fingerprint = '036967be53b57d23'
ORDER BY ce.created_at DESC
LIMIT 20;
```

3. Reproduzir em `http://localhost:5173/` com a mesma conta/círculos; DevTools → componente em `client_errors.properties.component`.
4. Se `component` apontar filho do dashboard (`DashboardDayToDaySection`, `DayToDayDiscoveryHub`, etc.), abrir bug tier 1 com stack ou teste de regressão; se volume >5 usuários/24h, escalar para incidente (`RUNBOOK_ALERTS.md`).

## Console

http://127.0.0.1:3013?tab=incidentes&investigationId=1b55aea2-b996-44a4-bdd3-6677aaf44d26&reportId=7f7f6760-52e7-43f4-9070-b9ed28c4e1ba

## Remediação (resumo agente)

Rascunho tier 0: fingerprint genérico de React Error Boundary na home (`/`). Categoria `technical_bug` confirmada; próximo passo é SQL acima + `properties.component` para localizar o componente que lança.
