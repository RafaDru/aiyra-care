# Investigação — 68feeb63-b205-4dfc-88b6-998e039407b2

- **investigationId:** 07c77172-37d2-4cb3-b396-432dba3d9545
- **Categoria:** technical_bug
- **Rota:** `/`
- **Fingerprint:** `036967be53b57d23` → `ui|ui_boundary|error` (SHA-256 truncado de `feature|errorKind|errorCode`)
- **Tier:** 0 (rascunho automático)
- **Gatilho:** auto
- **Ambiente:** integration (`environment.deploymentTier`)
- **API:** http://127.0.0.1:3010 (`environment.apiPublicUrl`)
- **reportId:** 68feeb63-b205-4dfc-88b6-998e039407b2
- **consentTechnical:** true · **consentProfileAccess:** false
- **Notas ops:** *(nenhuma no payload)*

## Decodificação do fingerprint

| Campo | Valor |
|-------|--------|
| `feature` | `ui` (fixo em `reportUiBoundaryError`) |
| `error_kind` | `ui_boundary` |
| `error_code` | `error` (`sanitizeErrorCode` de `error.name`, tipicamente `Error`) |

Referências:

- `packages/web/src/lib/client-error-fingerprint.ts` — `computeClientErrorFingerprint`
- `packages/web/src/lib/client-errors.ts` — `reportUiBoundaryError` usa `feature: 'ui'`, não `deriveFeatureFromRoute`
- `packages/web/src/components/errors/AppErrorBoundary.tsx` — `componentDidCatch` → telemetria

A rota `/` mapeia para **`Dashboard`** (`packages/web/src/pages/dashboard.tsx` via `App.tsx`), mas o fingerprint **não** reflete `dashboard` — só `ui`, o que dificulta a matriz Saúde por feature no console.

## Revisão de categoria

- **Adequada:** `technical_bug` — falha capturada pelo error boundary (UI não tratada), não confusão de UX nem dado incorreto sem evidência.

## Hipóteses

1. **Exceção de render no dashboard (principal)** — O usuário estava em `/` (lista de perfis). Falhas de API em `load()` viram `loadError` (Alert) e **não** disparam error boundary. O fingerprint indica falha capturada pelo **`AppErrorBoundary` raiz** (`main.tsx`): algum filho lançou durante render/commit (ex.: bloco «Hoje» — `DashboardDayToDaySection` → `WalletTodayPanel` / `useAvaPatientLens`; `PatientCard` com `ageCategory` atípico; ou chrome global em `AppLayout` — Ava, círculos de cuidado, banner degradado).
2. **Padrão recorrente em integration** — O mesmo fingerprint aparece em múltiplos `support_reports` na rota `/` (investigações correlatas `7cae1d6f`, `96a2e898`, `1b55aea2`, `225ff8f1`, este `07c77172`), sugerindo regressão ou dependência compartilhada, não dado clínico isolado.
3. **Granularidade de telemetria insuficiente para triagem remota** — `properties.component` no PG (primeira linha do `componentStack`) identifica o componente; o agente Tier 0 não acessa PG. Próximo passo humano obrigatório.

## Evidências no repo

| Artefato | Observação |
|----------|------------|
| `App.tsx` L42 | `Route path="/"` → `<Dashboard />` |
| `AppErrorBoundary.tsx` L26–28 | Reporta `ui_boundary` com `error.name \|\| 'Error'` |
| `dashboard.tsx` | Lista pacientes + modais; erros de fetch tratados sem boundary |
| `DashboardDayToDaySection.tsx` | Monta `WalletTodayPanel` quando há pacientes — candidato a repro |
| `docs/ops/SUPPORT_REPORTS.md` | Triagem `technical_bug` → fingerprint + rota |
| PR #76 (draft, correlato) | Investigação espelho `7cae1d6f` — mesmo fingerprint e hipóteses |

## Próximo passo humano

1. No host **integration**, abrir o console (link abaixo) e o detalhe do chamado `68feeb63…`.
2. Com acesso PG autorizado, correlacionar erros na janela do reporte (**sem PHI**):

```sql
SELECT ce.fingerprint, ce.feature, ce.error_kind, ce.error_code,
       ce.properties->>'component' AS component_hint,
       ce.created_at
FROM support_reports sr
JOIN client_errors ce
  ON ce.account_id = sr.account_id
 AND ce.fingerprint = '036967be53b57d23'
 AND ce.created_at BETWEEN sr.created_at - INTERVAL '15 minutes'
                       AND sr.created_at + INTERVAL '5 minutes'
WHERE sr.id = '68feeb63-b205-4dfc-88b6-998e039407b2';
```

3. Reproduzir em http://localhost:5173/ (stack integration) com DevTools → stack no overlay do boundary; confirmar `component_hint`.
4. Se `component_hint` apontar para `WalletTodayPanel` / Ava / círculos — issue de engenharia com stack; **Tier 1** só se fix local cabe em `TIER1_GATES.md`.
5. Backlog ops: enriquecer fingerprint com rota (`deriveFeatureFromRoute`) ou `feature="dashboard"` no boundary do layout autenticado.

## Console

http://127.0.0.1:3013?tab=incidentes&investigationId=07c77172-37d2-4cb3-b396-432dba3d9545&reportId=68feeb63-b205-4dfc-88b6-998e039407b2

## Remediação (resumo agente — Tier 0)

Fingerprint mapeado para React Error Boundary genérico na home (`/` → Dashboard). Categoria `technical_bug` confirmada. Próximo passo: SQL acima + repro usando `properties.component` no PG; sem alteração de código neste run.
