# Investigação — adcf1aa0-7ee4-41f2-b0f3-659bcef3fc18

- **investigationId:** 96a2e898-9d36-4495-82e6-76eb17fc555e
- **Categoria:** `technical_bug`
- **Rota:** `/`
- **Fingerprint:** `036967be53b57d23` → `ui|ui_boundary|Error` (SHA-256 truncado de `feature|errorKind|errorCode`)
- **Tier:** 0 (rascunho automático)
- **Gatilho:** auto
- **Ambiente:** integration (`environment.deploymentTier`)
- **API:** http://127.0.0.1:3010 (`environment.apiPublicUrl`)
- **Consentimento técnico:** sim · perfil: não
- **Notas ops:** *(não informadas no payload)*

## Resumo

Chamado manual na **home autenticada** (`/`, componente `Dashboard`) com bundle técnico apontando o fingerprint mais genérico do app: exceção React capturada pelo `AppErrorBoundary`, telemetria com `feature=ui`, `errorKind=ui_boundary`, `errorCode=Error` (nome da exceção). O mesmo fingerprint aparece em outros reportes recentes (ex.: investigation `7cae1d6f`, `1b55aea2`) — agrupa **qualquer** falha de render sob o boundary global, não só o dashboard.

## Hipóteses

1. **Erro de render no subtree da home (Dashboard / AppLayout)** — Rota `/` mapeia para `packages/web/src/pages/dashboard.tsx` dentro de `AppLayout` (Ava dock, círculos de cuidado, listagem de pacientes). Qualquer `throw` durante render ou lifecycle não tratado dispara `AppErrorBoundary` → fingerprint fixo acima. **Probabilidade: alta** (rota do chamado + padrão clássico de boundary).
2. **Telemetria agrega demais (`feature=ui` fixo)** — `reportUiBoundaryError` ignora `deriveFeatureFromRoute`; todos os boundary errors colapsam em `036967be53b57d23`, dificultando triagem só pelo fingerprint. A pista útil fica em `client_errors.properties.component` (primeira linha do `componentStack`). **Probabilidade: média** (não explica sozinho o bug, mas explica recorrência cross-rota).
3. **Estado auth/compliance ou dados vazios/inconsistentes** — Dashboard depende de `useAuth`, `useActiveCareCircle`, `api.patients.list()` e `api.careCircles.dashboard()`; falhas de API viram `loadError` (Alert), não boundary — hipótese de boundary exige bug de render (ex.: assumir campo não nulo). **Probabilidade: baixa/média** até ver `diagnostic_context` / stack no PG.

## Evidências no repo

| Item | Onde |
|------|------|
| Fingerprint `036967be53b57d23` | `computeClientErrorFingerprint('ui','ui_boundary','Error')` — ver `packages/web/src/lib/client-error-fingerprint.ts` |
| Emissão do evento | `reportUiBoundaryError` → `feature: 'ui'`, `errorKind: 'ui_boundary'`, `errorCode` = `error.name` — `packages/web/src/lib/client-errors.ts` |
| Captura UI | `AppErrorBoundary.componentDidCatch` — `packages/web/src/components/errors/AppErrorBoundary.tsx` |
| Rota `/` | `<Route path="/" element={<Dashboard />} />` — `packages/web/src/App.tsx` |
| Triagem categoria | `technical_bug` + fingerprint — `docs/ops/SUPPORT_REPORTS.md` |
| Playbook mensagem | `FEATURE_MESSAGES.ui.ReactError` — `packages/web/src/lib/client-error-playbook.ts` |

## Próximo passo humano

1. **Bundle (ambiente integration, sem expor PHI):**

```sql
SELECT id, route, category, created_at,
       diagnostic_context->'recentClientErrors' AS errors
FROM support_reports
WHERE id = 'adcf1aa0-7ee4-41f2-b0f3-659bcef3fc18';
```

2. **Correlacionar fingerprint + componente:**

```sql
SELECT fingerprint, error_code, route, properties, created_at
FROM client_errors
WHERE fingerprint = '036967be53b57d23'
  AND created_at BETWEEN '2026-09-16T03:12:00Z' AND '2026-09-16T03:32:00Z'
ORDER BY created_at DESC
LIMIT 20;
```

3. **Repro local:** login → `/` → DevTools console antes do boundary; se `properties.component` apontar componente filho, bissectar render (Dashboard vs `AvaGlobalDock` vs seletor de círculo).
4. **Melhoria produto (Tier 1 futuro, se recorrente):** incluir rota ou `deriveFeatureFromRoute` no fingerprint de boundary, ou enriquecer `errorCode` além de `Error` genérico — fora do escopo Tier 0 deste run.

## Categoria (revisão agente)

Categoria **confirmada** como `technical_bug`: fingerprint de `client_errors` + consentimento técnico; não há indício de `incorrect_data` ou `ux_confusion` sem descrição livre.

## Console

http://127.0.0.1:3013?tab=incidentes&investigationId=96a2e898-9d36-4495-82e6-76eb17fc555e&reportId=adcf1aa0-7ee4-41f2-b0f3-659bcef3fc18

## Remediação (resumo agente)

Tier 0: mapeado fingerprint para React Error Boundary global (`ui|ui_boundary|Error`); rota `/` → Dashboard; próximo passo é inspecionar `diagnostic_context` / `properties.component` no PG integration e reproduzir render failure. Sem alteração de código neste run.
