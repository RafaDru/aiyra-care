# Investigação — adcf1aa0-7ee4-41f2-b0f3-659bcef3fc18

- **investigationId:** 96a2e898-9d36-4495-82e6-76eb17fc555e
- **Categoria:** `technical_bug`
- **Rota:** `/`
- **Fingerprint:** `036967be53b57d23` → `ui|ui_boundary|Error` (SHA-256 truncado)
- **Tier:** 0 (rascunho automático)
- **Gatilho:** auto
- **Ambiente:** integration (`environment.deploymentTier`)
- **API:** http://127.0.0.1:3010 (`environment.apiPublicUrl`)
- **Consentimento técnico:** sim (`consentTechnical: true`)
- **Notas ops:** *(ausente no payload)*

## Hipóteses

1. **Falha de render React na árvore do Dashboard (principal)** — Rota `/` mapeia para `packages/web/src/pages/dashboard.tsx` (`App.tsx`). O fingerprint não usa `deriveFeatureFromRoute('/')` (`dashboard`); erros capturados pelo `AppErrorBoundary` chamam `reportUiBoundaryError` com `feature: 'ui'` e `errorCode` = `error.name` (`Error` genérico). Indica `componentDidCatch` no boundary raiz, não falha de API tratada por `loadError` no Dashboard.
2. **Subcomponente ou dado inconsistente no grid de pacientes** — `PatientCard` assume `patient.name` e `patient.ageCategory` válidos (`charAt(0)`, `CATEGORY_CONFIG[ageCategory]`). Dado corrompido ou campo nulo após migração/sync poderia lançar durante render; o bundle técnico (PG) traria `properties.component` na linha `client_errors` correspondente.
3. **Ruído de integração / múltiplos chamados correlatos** — O mesmo fingerprint aparece em outros reportes integration na rota `/` (triagem ops). Pode ser edge case real **ou** repetição de testes manuais «Reportar problema» sem descrição; confirmar volume por fingerprint antes de escalar a incidente (>5 contas/24h — `SUPPORT_REPORTS.md`).

## Evidências no repo

| Verificação | Resultado |
|-------------|-----------|
| Decodificação fingerprint | `computeClientErrorFingerprint('ui','ui_boundary','Error')` → `036967be53b57d23` — `client-error-fingerprint.ts` |
| Telemetria ui_boundary | `reportUiBoundaryError` → feature fixa `ui` — `client-errors.ts` |
| Boundary | `AppErrorBoundary.componentDidCatch` reporta `error.name \|\| 'Error'` — `AppErrorBoundary.tsx` |
| Rota `/` | `Dashboard` em `App.tsx` (RequireAuth + RequireCompliance + AppLayout) |
| Triagem categoria | `technical_bug` + fingerprint → fila Engenharia — `docs/ops/SUPPORT_REPORTS.md` |
| Playbook Tier 0 | Sem PR / sem alteração de produto neste run |

## Próximo passo humano

1. **Bundle PG** (host integration, autorizado):

```sql
SELECT id, route, category, created_at,
       diagnostic_context->'recentClientErrors' AS errors
FROM support_reports
WHERE id = 'adcf1aa0-7ee4-41f2-b0f3-659bcef3fc18';
```

```sql
SELECT fingerprint, error_code, feature, route, properties, created_at
FROM client_errors
WHERE fingerprint = '036967be53b57d23'
  AND created_at > NOW() - INTERVAL '7 days'
ORDER BY created_at DESC
LIMIT 20;
```

2. **Repro local:** `npm run up` → login → `/` → DevTools; se boundary aparecer, inspecionar stack em `properties.component` no POST de telemetria.
3. **Correção candidata (Tier 1 futuro):** endurecer `PatientCard`/Dashboard contra campos nulos; enriquecer fingerprint ui_boundary com rota real (`deriveFeatureFromRoute`) — fora do escopo Tier 0.
4. **Escalada:** se query 2 mostrar >5 `account_id` distintos em 24h, abrir incidente ops.

## Console

http://127.0.0.1:3013?tab=incidentes&investigationId=96a2e898-9d36-4495-82e6-76eb17fc555e&reportId=adcf1aa0-7ee4-41f2-b0f3-659bcef3fc18

## Remediação (resumo agente)

Fingerprint `036967be53b57d23` = erro React genérico no boundary raiz (`ui|ui_boundary|Error`) com usuário na home `/`. Categoria `technical_bug` confirmada. Tier 0: rascunho para triagem; próximo passo é ler `diagnostic_context` + `client_errors.properties.component` no PG integration antes de patch.
