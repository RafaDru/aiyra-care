# Investigação — a9cb7f94-a13f-43af-8ab4-d4aec53b6213

- **investigationId:** `051d1a71-ffc9-4e68-9802-09054bb9a2c3`
- **Categoria:** `technical_bug`
- **Rota:** `/`
- **Fingerprint:** *(ausente no webhook — `topFingerprint: null`)*
- **Tier:** 0 (rascunho automático)
- **Gatilho:** `auto`
- **Ambiente:** `integration` (`environment.deploymentTier`)
- **API:** `http://127.0.0.1:3010` (`environment.apiPublicUrl`)
- **Consentimento técnico:** sim (`consentTechnical: true`)
- **Acesso ao perfil:** não (`consentProfileAccess: false`)
- **Notas ops:** *(não enviadas no payload)*

## Contexto do payload

| Campo | Valor |
|-------|--------|
| `submittedAt` | `2026-09-16T02:33:57.594Z` |
| `playbook` | `support-report-tier0` |
| `analysisQueue.lane` | `development_support` |

Rota `/` no app autenticado resolve para o **Dashboard** (`packages/web/src/pages/dashboard.tsx`), dentro de `RequireAuth` + `RequireCompliance` + `AppLayout` (`packages/web/src/App.tsx`).

`topFingerprint` nulo com `consentTechnical: true` indica que `diagnostic_context.recentClientErrors` estava **vazio** no momento do envio (`extractTopFingerprint` em `support-report-dispatch.ts`) — não houve erro catalogado em `client_errors` nas últimas 24h (ou o bundle não os incluiu), apesar da categoria «bug técnico».

## Hipóteses

1. **Falha de API percebida sem telemetria correlata (principal)** — O Dashboard chama `api.patients.list()` e `api.careCircles.dashboard()` no mount. Falhas HTTP/rede em `request()` disparam `reportApiClientError` / `reportNetworkClientError` (`packages/web/src/lib/api.ts`), mas: (a) janela 24h já expirou para o submit de 16/09; (b) falha só em `careCircles.dashboard()` é **silenciosa** (`allSettled` — rejeição de círculos não seta `loadError`); (c) API offline no host de integração gera alerta inline (`loadError`) sem React boundary — usuário pode classificar como bug técnico sem fingerprint no webhook.
2. **Relato genérico / validação do fluxo de suporte** — Texto do dispatch: «Novo chamado: Bug técnico — /»; toast com `[?] Reporte manual` sugere descrição livre ausente ou não propagada ao agente. Em `integration`, chamados manuais na home são comuns em smoke do botão **Reportar problema** (`SupportReportModal` em `AppLayout.tsx`).
3. **Problema de UX ou dado incorreto mal categorizado (secundária)** — Home vazia (`Empty` + `DayToDayDiscoveryHub`) pode ser interpretada como «erro»; sem fingerprint e sem `consentProfileAccess`, o agente não valida dados clínicos. Reclassificar só após ler descrição no PG.

## Evidências no repo

| Área | Referência |
|------|------------|
| Rota `/` → Dashboard | `packages/web/src/App.tsx` (linha ~42), `packages/web/src/pages/dashboard.tsx` |
| Carga inicial | `load()` — `Promise.allSettled([api.patients.list(), api.careCircles.dashboard()])` |
| Erro de lista | `loadError` + `Alert` retry; **não** chama `reportClientError` no `catch` local (telemetria depende do `request()` subjacente) |
| Error boundary global | `AppErrorBoundary` em `main.tsx` → fingerprint típico `ui\|ui_boundary\|Error` (**não** presente neste ticket) |
| Dispatch webhook | `packages/api/src/application/support-report/support-report-dispatch.ts` — `topFingerprint` só dos `recentClientErrors` |
| Triagem categoria | `docs/ops/SUPPORT_REPORTS.md` — `technical_bug` → `client_errors` + rota |
| QA fluxo reporte | `docs/testing/suites/support-user-report.md`, e2e `support-user-report.spec.ts` |

## Próximo passo humano

1. **Console ops** — Abrir detalhe do chamado e ler descrição livre + `diagnostic_context` (ambiente integration autorizado):
   ```sql
   SELECT id, category, route, description, analysis_status,
          diagnostic_context->'recentClientErrors' AS errors,
          diagnostic_context->'recentProductEvents' AS events,
          diagnostic_context->'lastSyncFailure' AS sync_fail,
          created_at
   FROM support_reports
   WHERE id = 'a9cb7f94-a13f-43af-8ab4-d4aec53b6213';
   ```
2. **Reproduzir na home** — Com a mesma conta (se autorizado), abrir `/`, verificar Network para `GET /patients` e `GET /care-circles/...` (ou rota equivalente), e se há `loadError` na UI.
3. **Se houver erros no bundle** — Correlacionar fingerprint com `client_errors` e playbook em `client-error-playbook.ts`; escalar para Tier 1 só se correção óbvia nos gates.
4. **Se bundle vazio e descrição vaga** — Marcar como triagem manual / possível smoke; considerar pedir `operatorNotes` no re-disparo **Analisar** com passos de repro.

## Console

http://127.0.0.1:3013?tab=incidentes&investigationId=051d1a71-ffc9-4e68-9802-09054bb9a2c3&reportId=a9cb7f94-a13f-43af-8ab4-d4aec53b6213

## Remediação (resumo agente)

Tier 0: investigação documentada. Hipótese principal — bug percebido na home sem `client_errors` recentes no webhook; investigação humana via SQL/console e repro em `/`. Sem alteração de código nem PR neste run.
