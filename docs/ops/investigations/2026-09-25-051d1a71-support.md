# Investigação — a9cb7f94-a13f-43af-8ab4-d4aec53b6213

- **investigationId:** `051d1a71-ffc9-4e68-9802-09054bb9a2c3`
- **Categoria:** `technical_bug`
- **Rota:** `/`
- **Fingerprint:** *(ausente — `topFingerprint: null`)*
- **Tier:** 0 (rascunho automático)
- **Gatilho:** `auto`
- **Ambiente:** `integration` (`environment.deploymentTier`)
- **API:** `http://127.0.0.1:3010` (`environment.apiPublicUrl`)
- **Consentimento técnico:** sim (`consentTechnical: true`)
- **Acesso perfil:** não (`consentProfileAccess: false`)
- **Submetido em:** 2026-09-16T02:33:57.594Z
- **Notas ops:** *(não enviadas no payload)*

## Hipóteses

1. **Chamado de validação do fluxo «Reportar problema» (mais provável)** — Toast com prefixo `[?] Reporte manual`, rota `/` (dashboard autenticado) e ausência de `topFingerprint` indicam envio sem `client_errors` recentes na janela do bundle. Comum em smoke manual da feature `support-user-reports` no ambiente de integração local.
2. **Falha na home sem erro capturado em `client_errors`** — Se o relato for real: a dashboard dispara `api.patients.list()` e `api.careCircles.dashboard()` no mount (`packages/web/src/pages/dashboard.tsx`). Falha de API pode aparecer só como `loadError` (Alert) sem passar pelo pipeline de fingerprint se o usuário não gerou boundary error ou a janela de 15 min não incluiu o evento.
3. **Categoria correta mas evidência insuficiente para fix** — `technical_bug` permanece plausível, porém sem fingerprint, descrição (PHI — fora do escopo do agente) ou `operatorNotes` não há alvo de código único; triagem humana deve ler `diagnostic_context` no host de integração.

## Evidências no repo

| Verificação | Resultado |
|-------------|-----------|
| Mapeamento rota `/` | `packages/web/src/App.tsx` → `Dashboard` (`packages/web/src/pages/dashboard.tsx`) |
| Captura de rota no reporte | `SupportReportModal` envia `route: location.pathname` (`packages/web/src/components/support/SupportReportModal.tsx`) |
| `topFingerprint` null | `extractTopFingerprint` retorna `null` quando `diagnostic_context.recentClientErrors` está vazio (`support-report-dispatch.ts`) |
| Triagem `technical_bug` | `docs/ops/SUPPORT_REPORTS.md` — primeiro olhar: fingerprint + rota; sem fingerprint → correlacionar bundle PG |
| Feature / QA | `docs/features/support-user-reports.md`, suite `support-user-report` |

## Próximo passo humano

1. No host **integration** (`127.0.0.1:3010` / PG local), inspecionar o chamado (sem expor PHI em canais externos):

```sql
SELECT id, category, route, status, analysis_status, created_at,
       consent_technical,
       jsonb_array_length(COALESCE(diagnostic_context->'recentClientErrors', '[]'::jsonb)) AS n_client_errors,
       jsonb_array_length(COALESCE(diagnostic_context->'recentProductEvents', '[]'::jsonb)) AS n_events
FROM support_reports
WHERE id = 'a9cb7f94-a13f-43af-8ab4-d4aec53b6213';
```

2. Se `n_client_errors = 0` e a descrição for vaga ou de teste → **Resolver** como smoke / fechar sem engenharia.
3. Se houver descrição de falha na dashboard → reproduzir com DevTools (Network em `GET /patients`, care circles); checar `api.log` / `client_errors` na mesma `account_id` (query em `SUPPORT_REPORTS.md`).
4. Console: marcar análise concluída após leitura do artefato; re-disparar com **Analisar** + `operatorNotes` se precisar de nova rodada do agente.

## Console

http://127.0.0.1:3013?tab=incidentes&investigationId=051d1a71-ffc9-4e68-9802-09054bb9a2c3&reportId=a9cb7f94-a13f-43af-8ab4-d4aec53b6213

## Remediação (resumo agente)

Tier 0: rascunho de triagem — sem PR. Hipótese principal: reporte manual/smoke na home sem fingerprint; validar no PG e descrição do usuário antes de alterar código.
