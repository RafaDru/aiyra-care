# Investigação — a9cb7f94-a13f-43af-8ab4-d4aec53b6213

- **investigationId:** `051d1a71-ffc9-4e68-9802-09054bb9a2c3`
- **Categoria:** `technical_bug`
- **Rota:** `/`
- **Fingerprint:** *(ausente no webhook — `topFingerprint: null`)*
- **Tier:** 0 (rascunho automático)
- **Gatilho:** `auto`
- **Ambiente:** `integration` (`environment.deploymentTier`)
- **API:** `http://127.0.0.1:3010` (`environment.apiPublicUrl`)
- **Consentimento técnico:** sim (`consentTechnical: true`)
- **Notas ops:** *(não enviadas no payload)*

## Contexto

Chamado manual na **home autenticada** (`/`), submetido em 2026-09-16. O dispatch não trouxe fingerprint de `client_errors`, o que indica bundle técnico **sem** `recentClientErrors` populado (ou lista vazia) no momento do submit — diferente de outros tickets do mesmo dia com `036967be…` (`ui|ui_boundary|Error`).

## Hipóteses

1. **Falha de API/rede no carregamento da lista de pacientes (principal)** — O `Dashboard` chama `api.patients.list()` e `api.careCircles.dashboard()`; rejeição de `patients.list` preenche `loadError` com Alert e retry, **sem** obrigatoriamente gerar fingerprint no webhook (erro tratado no componente). Compatível com `technical_bug` + rota `/` + `topFingerprint` nulo.
2. **Percepção de “bug” sem exceção registrada** — Empty state, lentidão ou confusão com onboarding/círculos familiares; sem erros no bundle, triagem humana deve ler `diagnostic_context` (eventos + último sync) antes de priorizar engenharia.
3. **Instabilidade pontual do stack integration** — Se API `:3010` estava indisponível no host do usuário, o sintoma seria o mesmo Alert de carga; correlacionar timestamp do chamado com health/métricas locais (não inferível só pelo monorepo).

## Evidências no repo

| Item | Local |
|------|--------|
| Rota `/` → componente | `packages/web/src/App.tsx` — `Dashboard` dentro de `RequireAuth` + `RequireCompliance` |
| Carregamento e erro tratado | `packages/web/src/pages/dashboard.tsx` — `load()` + `loadError` Alert (linhas ~47–64, 175–184) |
| Fingerprint nulo no webhook | `packages/api/src/application/support-report/support-report-dispatch.ts` — `extractTopFingerprint` só lê `diagnosticContext.recentClientErrors` |
| Erros UI com fingerprint | `packages/web/src/components/errors/AppErrorBoundary.tsx` — `reportUiBoundaryError` (outros tickets do lote 2026-09-16) |
| Triagem `technical_bug` | `docs/ops/SUPPORT_REPORTS.md` — primeiro olhar `client_errors` + rota |

**Probe agente (VM cloud, 2026-09-25):** `http://127.0.0.1:3010/health` e `:3013` inacessíveis — esperado fora do host integration; investigação baseada em código/docs.

## Próximo passo humano

1. No host **integration**, abrir o console: [dashboard](http://127.0.0.1:3013?tab=incidentes&investigationId=051d1a71-ffc9-4e68-9802-09054bb9a2c3&reportId=a9cb7f94-a13f-43af-8ab4-d4aec53b6213).
2. Inspecionar bundle (sem expor PHI em canais externos):

```sql
SELECT id, route, category, created_at,
       jsonb_array_length(COALESCE(diagnostic_context->'recentClientErrors', '[]'::jsonb)) AS n_errors,
       diagnostic_context->'lastSyncFailure' AS last_sync,
       left(description, 200) AS description_excerpt
FROM support_reports
WHERE id = 'a9cb7f94-a13f-43af-8ab4-d4aec53b6213';
```

3. Se `n_errors = 0`: pedir reprodução com DevTools (Network em `GET /patients`) ou reclassificar para `ux_confusion` se for expectativa de produto.
4. Se houver erros API no bundle: seguir fingerprint/`api_path` e logs API no intervalo `created_at ± 15 min`.

## Console

http://127.0.0.1:3013?tab=incidentes&investigationId=051d1a71-ffc9-4e68-9802-09054bb9a2c3&reportId=a9cb7f94-a13f-43af-8ab4-d4aec53b6213

## Remediação (resumo agente)

Tier 0: rascunho de triagem — hipótese principal falha silenciosa no load do Dashboard sem fingerprint anexado; aguarda SQL + repro no ambiente integration. Sem PR.
