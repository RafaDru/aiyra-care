# Investigação — a9cb7f94-a13f-43af-8ab4-d4aec53b6213

- **investigationId:** `051d1a71-ffc9-4e68-9802-09054bb9a2c3` (`[inv:051d1a71]`)
- **Categoria:** `technical_bug`
- **Rota:** `/`
- **Fingerprint:** *(null no webhook — `consentTechnical: true`, sem `topFingerprint` no dispatch)*
- **Tier:** 0 (rascunho automático)
- **Gatilho:** `auto`
- **Ambiente:** `integration` (`environment.deploymentTier`)
- **API:** `http://127.0.0.1:3010` (`environment.apiPublicUrl`)
- **Notas ops:** *(ausentes no payload)*

## Contexto do payload

| Campo | Valor |
|-------|--------|
| `reportId` | `a9cb7f94-a13f-43af-8ab4-d4aec53b6213` |
| `submittedAt` | `2026-09-16T02:33:57.594Z` |
| `consentProfileAccess` | `false` |
| `playbook` | `support-report-tier0` |
| `analysisQueue.lane` | `development_support` |

`topFingerprint` vem de `diagnostic_context.recentClientErrors[0].fingerprint` (`extractTopFingerprint` em `support-report-dispatch.ts`). Valor **null** indica array vazio ou entrada sem fingerprint no momento do submit — não prova ausência de problema percebido pelo usuário.

## Mapeamento rota → código

| Rota | Componente |
|------|----------------|
| `/` (autenticado) | `packages/web/src/pages/dashboard.tsx` (`Dashboard`) |
| Boundary global | `packages/web/src/components/errors/AppErrorBoundary.tsx` (envolve app em `main.tsx`) |
| Bloco «Hoje» | `DashboardDayToDaySection` → `useAvaPatientLens`, `WalletTodayPanel` |
| Lista pacientes | `api.patients.list()` + `api.careCircles.dashboard()` em paralelo (`Promise.allSettled`) |

## Hipóteses (ranqueadas)

1. **Chamado sem erro técnico capturado no bundle (principal)** — Com `consent_technical`, o webhook deveria trazer o fingerprint dominante; `null` sugere que, na janela do bundle, não havia `recentClientErrors` persistidos (erro intermitente, dedupe 15s em `client-errors.ts`, ou falha de UX/API que só mostra `Alert` no dashboard sem `reportClientError`). **Próximo passo:** inspecionar `diagnostic_context` no PG deste `report_id` (ambiente integration autorizado).

2. **Mesmo cluster que outros reports `/` do dia 2026-09-16** — Vários chamados correlatos na fila usaram fingerprint `036967be53b57d23` = hash de `ui|ui_boundary|Error` (`reportUiBoundaryError` em `AppErrorBoundary.componentDidCatch`). Se este usuário viu a tela «Algo não funcionou como esperado», o bundle *deveria* listar esse fingerprint; a ausência no webhook favorece hipótese 1 ou submit antes/depois do crash. **Correlação:** buscar `client_errors` com `fingerprint = '036967be53b57d23'` e `route = '/'` na mesma conta (`report_id` apenas na query de suporte).

3. **Falha de carga no dashboard sem boundary React** — `Dashboard.load` rejeita em `api.patients.list()` e preenche `loadError` com mensagem i18n; falhas parciais de `careCircles.dashboard()` são engolidas (`fulfilled` vs `rejected`). Usuário pode classificar como bug técnico na home sem gerar `ui_boundary`. **Evidência:** `dashboard.tsx` linhas 47–63 — `reportApiClientError` depende do stack HTTP em `api.ts`, não do `loadError` local.

## Evidências no repo

| Item | Referência |
|------|------------|
| Extração fingerprint no dispatch | `packages/api/src/application/support-report/support-report-dispatch.ts` — `extractTopFingerprint` |
| Fingerprint `036967be…` | `ui` + `ui_boundary` + `Error` — `packages/web/src/lib/client-errors.ts` (`reportUiBoundaryError`) |
| Derivação feature por rota `/` | `deriveFeatureFromRoute` → `dashboard` (erros API na home usariam fingerprint diferente de boundary global) |
| Triagem categoria | `docs/ops/SUPPORT_REPORTS.md` — `technical_bug` → `client_errors` + rota |
| Playbook Tier 0 | `docs/ops/automations/support-report-investigator.prompt.md` |

## Revisão de categoria

**Adequada:** `technical_bug` — rota `/` (dashboard autenticado) e intent de falha técnica; sem evidência para `incorrect_data` ou `ux_confusion` só com metadados do webhook.

## Próximo passo humano

1. **Bundle (sem PHI na saída pública):**

```sql
SELECT id, category, route, consent_technical,
       diagnostic_context->'recentClientErrors' AS errors,
       diagnostic_context->'recentProductEvents' AS events,
       created_at
FROM support_reports
WHERE id = 'a9cb7f94-a13f-43af-8ab4-d4aec53b6213';
```

2. **Correlacionar erros ±15 min do submit** (substituir `account_id` após lookup interno autorizado):

```sql
SELECT sr.id, sr.created_at, ce.fingerprint, ce.error_kind, ce.error_code, ce.route
FROM support_reports sr
LEFT JOIN client_errors ce
  ON ce.account_id = sr.account_id
 AND ce.created_at BETWEEN sr.created_at - INTERVAL '15 minutes'
                       AND sr.created_at + INTERVAL '5 minutes'
WHERE sr.id = 'a9cb7f94-a13f-43af-8ab4-d4aec53b6213';
```

3. Se `036967be53b57d23` aparecer: reproduzir com DevTools — stack em `AppErrorBoundary` (`component` em `properties` do erro); priorizar componente filho que quebrou no dashboard (lente Ava / wallet / lista pacientes).

4. Se bundle vazio e sem `client_errors`: pedir ao usuário passos de repro ou reabrir com screenshot; considerar `ux_confusion` só após entrevista — não alterar categoria automaticamente.

5. **Console:** marcar análise concluída após revisar artefato; Tier 0 — **sem PR** nesta execução.

## Console

http://127.0.0.1:3013?tab=incidentes&investigationId=051d1a71-ffc9-4e68-9802-09054bb9a2c3&reportId=a9cb7f94-a13f-43af-8ab4-d4aec53b6213

## Remediação (resumo agente)

Rascunho Tier 0: home (`Dashboard`) mapeada; `topFingerprint` ausente → priorizar leitura de `diagnostic_context` e correlação com cluster `ui|ui_boundary|Error` (`036967be53b57d23`) ou falha silenciosa de carga de pacientes/círculos. Nenhuma alteração de código nesta execução.
