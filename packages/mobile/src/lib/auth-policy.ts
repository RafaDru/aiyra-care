/** Alinhado ao Supabase Auth (mínimo padrão) e mensagens web i18n. */
export const AUTH_PASSWORD_MIN_LENGTH = 6

export const AUTH_PASSWORD_HINT =
  `Use pelo menos ${AUTH_PASSWORD_MIN_LENGTH} caracteres. Evite senhas óbvias ou já usadas em outros sites.`
